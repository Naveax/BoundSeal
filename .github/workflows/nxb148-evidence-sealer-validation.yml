name: NXB-148 production evidence sealer

on:
  push:
    branches:
      - nxb-148-production-evidence-sealer
  pull_request:
    branches:
      - main
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/nxb-evidence-sealer/**"
      - ".github/workflows/nxb148-evidence-sealer-validation.yml"

permissions:
  contents: read

concurrency:
  group: nxb148-evidence-sealer-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: ${{ matrix.os }} evidence sealer
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-slim, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - name: Install pinned Rust
        uses: dtolnay/rust-toolchain@1.97.1
        with:
          components: rustfmt, clippy
      - name: Require canonical lockfile
        shell: bash
        run: |
          cargo generate-lockfile
          git diff --exit-code -- Cargo.lock
      - name: Formatting
        if: runner.os == 'Linux'
        run: cargo fmt --all -- --check
      - name: Check
        run: cargo check -p nxb-evidence-sealer --all-features --locked
      - name: Clippy
        run: cargo clippy -p nxb-evidence-sealer --all-targets --all-features --locked -- -D warnings
      - name: Tests
        run: cargo test -p nxb-evidence-sealer --all-features --locked -- --test-threads=1
