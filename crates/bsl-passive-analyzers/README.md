# bsl-passive-analyzers

Networkless passive checks for BSL-24 through BSL-29: security headers, cookie flags, TLS metadata, redirects, CORS and cache policy.

Analyzers consume bounded observations and emit redacted findings. They do not send requests, mutate parameters, enumerate identifiers or access secrets.
