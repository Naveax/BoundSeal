# nxb-content-analysis

Networkless contracts for NXB-16 through NXB-19: strict media-type and charset handling, bounded content-encoding accounting, structured document extraction and canonical URL/form discovery graphs.

The crate never fetches discovered URLs and never executes HTML, scripts, XML entities or browser code.
