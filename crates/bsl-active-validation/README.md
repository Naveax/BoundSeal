# nxb-active-validation

Networkless P4 contracts for capability-bound inert mutation, NXB-owned object cleanup, bounded differential observations and repeatability-based finding validation.

The crate does not implement network access, exploit payloads, command execution, credential attacks or arbitrary writes. Mutations are deterministic inert markers, and writes are permitted only for ownership-ledgered objects with explicit cleanup recipes.
